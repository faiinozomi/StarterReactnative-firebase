# StarterReactnative-firebase
learning using react-native + firebase

-- npm install -g expo-cli

-- expo init <{projectname}>     
ex. expo init ReactnativeFirebase

-- npm install --save firebase

-- cd <{projectname}>

-- npm start
